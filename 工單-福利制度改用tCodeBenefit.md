# 福利制度改以 tCodeBenefit 代碼表為單一資料來源（後端）

## 一、需求位置
1. 福利制度　　https://recruit.1111.com.tw/company/welfare
2. 公司頁預覽　https://www.1111.com.tw/corp/previewNew

## 二、需求背景
tCodeBenefit 代碼表已重整完成，現可判斷項目是否為「法定項目」。
目前福利項目的名稱、類別、排序未以代碼表為準（前端另行維護），導致代碼表異動時前後端顯示不一致，且新增項目需改程式。本單調整後端 API，改以代碼表為單一資料來源。

## 三、需求說明（後端）
1. API 改為直接自 tCodeBenefit 取得【名稱】【順序】【類別名稱】，不再由程式內寫死或另表維護。
2. 若該項目具有「法定項目」標記，其類別一律歸為「法定項目」，不再顯示其原本類別。
3. 分類與排序由後端組好後回傳，前端僅依序呈現，不自行判斷分類。
4. 上述兩個需求位置共用同一支 API 與同一套分類規則，顯示結果需一致。

## 四、資料規則
1. 分類歸屬：有法定標記 → 「法定項目」；無標記 → 依代碼表類別名稱。
2. 分類排序：「法定項目」固定置頂，其餘分類依代碼表順序。
3. 項目排序：各分類內依代碼表順序欄位由小到大。
4. 僅回傳代碼表中「啟用中」的項目；停用項目不回傳。
5. 該分類下無任何項目時，不回傳該分類（前端不顯示空標題）。
6. 公司既有資料若對應到已停用之代碼，一律不顯示。　※ 送單前請確認

## 五、API 回傳格式（範例）
```json
{
  "categories": [
    { "categoryName": "法定項目", "isStatutory": true,
      "items": [ { "code": "B001", "name": "勞工保險" },
                 { "code": "B002", "name": "全民健保" } ] },
    { "categoryName": "獎金福利", "isStatutory": false,
      "items": [ { "code": "B101", "name": "年終獎金" } ] }
  ]
}
```
※ 實際欄位命名由後端與前端確認後定案，惟分類與排序須由後端決定。

## 六、驗收條件
1. 於代碼表新增一筆項目並設為啟用 → 兩個頁面皆自動出現，無需改程式。
2. 將某項目的法定標記取消 → 該項目由「法定項目」移回其原類別。
3. 將某項目停用 → 兩個頁面皆不再顯示該項目。
4. 調整代碼表順序欄位 → 兩個頁面顯示順序同步變更。
5. 「法定項目」分類於兩個頁面皆顯示於最上方。

## 七、附件
資料流與 API 格式示範頁：`benefit-codetable-demo.html`
（可直接以瀏覽器開啟，調整左側代碼表即可看到 API 回應與畫面連動結果）

## 八、備註
前端改為使用 API 資料之部分，另開一張前端單處理。　※ 送單前請確認
