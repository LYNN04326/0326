## 一、需求位置

* 福利制度
  https://recruit.1111.com.tw/company/welfare
* 公司頁預覽
  https://www.1111.com.tw/corp/previewNew
* ❓待確認：正式公司頁（求職端對外）是否一併納入？若未納入，預覽與正式頁顯示將不一致。

---

## 二、需求說明

1. API 改為直接自 `tCodeBenefit` 取得【名稱】【順序】【類別名稱】【是否法定】，不再另外維護一份清單（程式內寫死或存於其他資料表皆不可）。
2. 法定項目以 **`BenefitKind`** 判定：`0` ＝ 預設值、`1` ＝ 法定項目，共 17 項。
   ⚠️ **`tCodeBenefit` 目前尚無此欄位**，需由後端新增；欄位值來源為 [REF] 系統代碼表 §6 的 `BenefitKind` 欄。
3. 若該項目 `BenefitKind = 1`，其類別一律歸為「法定項目」，**不再顯示於原本類別**。
4. 分類與排序由後端組好後回傳，前端僅依序呈現，不自行判斷分類。
5. 送出檢核 Modal 內的項目名稱，一併改為由 API 帶出（現行為前端寫死，且為舊名稱）。
6. 上述需求位置共用同一支 API 與同一套分類規則，顯示結果需一致。

### 畫面示意

**圖 1｜福利制度頁（改版後）**

![圖 1｜福利制度頁](mockup-img/01-福利制度頁.png)

> 版面與現行正式站相同，本次**不動版面**，只把資料來源從程式寫死改為 API。
> 「法定項目」為 `BenefitKind = 1` 聚合出的虛擬分類，17 項抽出後不在原分類重複 —— 休假類只剩 3 項、保險類只剩 7 項。

**圖 2｜Chip 狀態**

![圖 2｜Chip 狀態](mockup-img/02-Chip狀態.png)

> 法定項目與非法定項目使用**完全相同的 Chip 樣式**，差別只在被放進「法定項目」區塊。

**圖 3｜法定項目：全部已勾 vs 全部未勾**

![圖 3｜法定項目已勾未勾](mockup-img/03-法定項目已勾未勾.png)

> 已於正式站實測：17 項**全部未勾**時，送出 Modal 列出 **15 項**（排除週休二日 1007、哺乳室 1702）；**全部勾選時不跳 Modal，直接送出**。

---

## `資料規則`

1. **分類歸屬**：`BenefitKind = 1` → 「法定項目」；`BenefitKind = 0` → 依 `CodeNameB`（代碼表類別名稱）。
2. **分類排序**：「法定項目」固定置頂，其餘分類依 `CodeNo` 由小到大（1000 其他類 → 1900 補助類）。
   ❓待確認：正式站現行「其他類」排在**最後**，但其 `CodeNo` 為 **1000（最小）**。照本條規則其他類會移到**第一個**（法定項目之後），與現況不同。請擇一：
   （a）照 `CodeNo` 排，接受畫面順序變動；（b）其他類固定置底，其餘依 `CodeNo`；（c）改 `CodeNo`（須另走代碼需求表提單）。
3. **項目排序**：各分類內依 `CodeNo` 由小到大。（`tCodeBenefit` 無獨立順序欄位，`CodeNo` 本身即為順序）
4. **啟用範圍**：以 `tCodeBenefit` 現有資料為準，表中不存在之代碼即視為停用（現行已停用代碼採直接移除，如缺號 1503、1705）。
5. **空分類**：該分類下無任何項目時，不回傳該分類（前端不顯示空標題）。
6. **既有資料相容**：公司既有資料若對應到已停用／表中不存在之代碼，一律略過不顯示，且不得因此報錯。

---

## `送出檢核相關旗標`

> 以下為 B.1.5 規格書既有規則所需，與顯示規則同批處理。
> `BenefitKind` 只有 0／1 兩個值，無法同時表達「法定（17 項）」「送出必勾（2 項）」「未勾提示（15 項）」三種範圍，故建議由**後端計算後回傳布林欄位**，DB 不需為此再加欄位。

7. **`requireSubmit`**：送出前強制校驗之項目 —— **勞保（1001）、健保（1002）共 2 項**，未勾選時不得送出。
8. **`requireCheckAlert`**：未勾選時需列入提示 Modal 之項目 —— **法定 17 項中的 15 項**。

   | CodeNo | 名稱 | requireCheckAlert |
   |---|---|---|
   | 1007 | 週休二日 | **false** |
   | 1702 | 哺乳室 | **false** |
   | 其餘 15 項 | — | **true** |

**圖 4｜送出確認 Modal（依正式站實際樣式）**

![圖 4｜送出Modal](mockup-img/05-送出Modal.png)

9. **Modal 動態清單規則**
   - 觸發時機：按「確定送出」時，`requireCheckAlert = true` 的項目有任一未勾選。
   - 內容：只列出該廠商**未勾選**的項目，名稱取自 API（`CodeNameA`），以「、」串成單行，紅字 `#a30021`。
   - **名稱不得於程式內寫死**（現行寫死且為舊名稱，如「勞保（5人以上強制納保）」）。
   - **排序：依 `CodeNo` 由小到大**。
     🔴 現行為另外寫死的順序 —— 前 12 項大致依 `CodeNo`，但勞退提繳金(1308)、職災保險(1309)、就業保險(1311) 被排到最後且為**倒序**，與畫面上 chip 順序也不一致，本次一併修正。

**圖 5｜初始化提示 Modal**

![圖 5｜初始化Modal](mockup-img/06-初始化Modal.png)

10. 進入頁面時若有法定項目未勾選，顯示「重要提醒」Modal。與送出 Modal 的差異：標題不同、**僅一顆「確定」**、無取消與 ✕。項目名稱同樣需由 API 帶出。
    ※ 本圖未取得正式站實際畫面（存檔時未觸發），版面依 B.1.5 規格書與 Axure 原型重建，**實際樣式以站上為準**。
    ❓待確認：勞保／健保未勾選時的提醒文案（需主管確認）。

---

## `驗收標準`

1. 於 `tCodeBenefit` 新增一筆代碼後，兩個需求位置不需改任何程式即應顯示該項目。
2. 於 `tCodeBenefit` 修改任一項目名稱或分類後，兩個需求位置顯示同步變更。
3. 將某項目的 `BenefitKind` 由 0 改為 1 後，該項目自動移入「法定項目」分類，且不再出現於原分類。
4. 送出檢核 Modal 內的項目名稱，隨 `tCodeBenefit` 名稱變更同步變動。
5. 廠商既有資料含已停用代碼時，頁面正常顯示且不報錯。

---

## 三、需求檔案

* [求才求職代碼表](https://hackmd.io/QVkCoCgXQYihoIILj9cUFw) — 福利代碼清單與 `BenefitKind` 標記（§6）
* [B.1.5 福利制度規格書](https://hackmd.io/@1111-jobdocs/HJvhE8BFWe) — 畫面規則、Modal 行為、法定項目 17 項清單

---

## 四、資安考量

**【Security Checklist】**

- [ ] 是否涉及個資 (PII) 或敏感資料蒐集/處理？
- [ ] 是否新增或修改身分驗證 (Login) 或權限 (Permission) 邏輯？
- [ ] 是否有檔案上傳功能？
- [ ] 是否串接新的外部 API 或開放對外 API？
- [ ] 是否涉及金流交易？
- [x] 無資安議題

---

## 五、其他

### 開發項目檢核

- [x] [系統發展生命週期控制措施檢核表](https://docs.google.com/document/d/1RBzpcz7e_cSdFzUPaZVQUAEA8tMOY4OS/edit)
- [x] [資通系統防護基準自評表](https://docs.google.com/spreadsheets/d/12XGBEfiFIgqhuyQdYwoUCQVHjfS1Xv8F/edit?gid=775107332#gid=775107332)

### 備註

1. `tCodeBenefit` 結構與其他 TCode 表不同：無 `CodeNameC`，G 欄為 `CodeDescript`；僅兩層（分類→項目），`CodeType` 為 1＝分類、2＝項目。
2. DB 現存名稱**已為新版名稱**，前端改吃 API 後即為新名稱，不需額外改名作業；各端需自查是否有 hardcode 舊名稱或以名稱字串比對之邏輯。

**圖 6｜改版後項目名稱會變（部分範例）**

![圖 6｜名稱對照](mockup-img/04-名稱對照.png)

3. ❓待確認：求職端是否需多語系？`tCodeBenefit` 具 `CodeNameA_EN／VI／TH／ID` 四語系欄位，若需支援，API 應規劃語系參數。
4. ❓待確認：送出後的 E-mail 通知規則（後端確認）。
5. 除了「`BenefitKind = 1` 的項目抽出放第一個分類」這一條之外，前端**不做任何分類判斷** —— 分類名稱、分類順序、項目名稱、項目順序全部照 API 回傳值依序呈現。

---

## 六、待確認事項彙總

| # | 項目 | 需誰確認 |
|---|---|---|
| 1 | 正式公司頁（求職端對外）是否一併納入 | 主管 |
| 2 | 「其他類」排序（`CodeNo` 最小卻排最後） | 主管 |
| 3 | 勞保／健保未勾選時的提醒文案 | 主管 |
| 4 | 求職端是否需多語系 | 主管 |
| 5 | 送出後 E-mail 通知規則 | 後端 |
| 6 | `tCodeBenefit` 新增 `BenefitKind` 欄位（需走代碼需求表提單） | 系統管理部 |
