# 專案慣例

## 回覆格式

- 使用者要程式碼時，**一定要在回覆訊息中附上完整的純 HTML 原始碼文字**（放在 ```html 程式碼區塊中），
  而不是只把檔案寫到磁碟或只描述改了什麼。即使同時有建立檔案，也要把完整內容貼出來。
- 回覆使用繁體中文。

## 程式碼風格

- HTML 頁面以單一自含檔案為主：CSS 與 JS 內嵌，不依賴外部 CDN。
- 介面文字使用繁體中文。

## 任務卡／工單

- 任務卡的需求說明採「因…，故…」句型：先用一至兩句交代原因（例如「因客服反映：…」、
  「因求職端同步文字需求」），再接一句要調整什麼，不展開細節規格。
- 細節規格（元件、選項、樣式、驗收條件）寫在工單，不寫進任務卡。
- 版面異動附截圖，並用紅框標示要新增或修改的位置。
