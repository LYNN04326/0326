# 專案慣例

## 回覆格式

- 使用者要程式碼時，**一定要在回覆訊息中附上完整的純 HTML 原始碼文字**（放在 ```html 程式碼區塊中），
  而不是只把檔案寫到磁碟或只描述改了什麼。即使同時有建立檔案，也要把完整內容貼出來。
- 回覆使用繁體中文。

## 程式碼風格

- HTML 頁面以單一自含檔案為主：CSS 與 JS 內嵌，不依賴外部 CDN。
- 介面文字使用繁體中文。

## 任務卡／工單（求才）

- 任務卡的需求說明採「因…，故…」句型：先用一至兩句交代原因（例如「因客服反映：…」、
  「因求職端同步文字需求」），再接一句要調整什麼，不展開細節規格。
- 細節規格（元件、選項、樣式、驗收條件）寫在工單，不寫進任務卡。
- 工單只寫「要做什麼」與規格，不寫需求原因；原因一律留在任務卡。
- 版面異動附截圖，並用紅框標示要新增或修改的位置。
- 工單不自行撰寫樣式規格（色碼、圓角、字級、間距等）：一律寫「沿用站上既有設計／既有元件」，
  必要時用截圖圈出參考位置即可，細節交由工程師依站上規範實作。
- 「需求檔案」欄只列**需另外開啟的附件**（工單內文寫「見附檔」時）；示意圖若已內嵌在需求說明中，
  此欄留白或寫「無」即可，不必重複列檔名。

## 求才工單模板（固定結構，一律照這個順序產出）

```
# 【前端／後端】頁面：本次調整標題

---
## 一、需求位置
頁面名稱、適用範圍（各分頁以 markdown 連結列出）

---
## 二、需求說明
一句話講要做什麼（不寫原因），接著依序放：
設計說明（樣式沿用來源、是否有 Figma、切版要求）→ 需求內容（元件、選項表格、互動）
→ 對應語法（```html 區塊，示意為主）→ 不得更動 → 驗收條件 → 本工單範圍外

---
## 三、需求檔案
示意圖、PDF 等附件檔名列表

---
## 四、資安考量
 **【Security Checklist】**
- [ ]  是否涉及個資 (PII) 或敏感資料蒐集/處理？
- [ ]  是否新增或修改身分驗證 (Login) 或權限 (Permission) 邏輯？
- [ ]  是否有檔案上傳功能？
- [ ]  是否串接新的外部 API 或開放對外 API？
- [ ]  是否涉及金流交易？
- [x]  無資安議題

---
## 五、其他
### 開發項目檢核
 - [x] [系統發展生命週期控制措施檢核表](https://docs.google.com/document/d/1RBzpcz7e_cSdFzUPaZVQUAEA8tMOY4OS/edit)
 - [x] [資通系統防護基準自評表](https://docs.google.com/spreadsheets/d/12XGBEfiFIgqhuyQdYwoUCQVHjfS1Xv8F/edit?gid=775107332#gid=775107332)
```

- 工單以 Markdown 撰寫：UI 元件名稱用行內 `code`、HTML 用 ```html 區塊、示意圖用 `![說明](網址)` 內嵌。
- 前端與後端分開開單；非本工單範圍的項目列在「本工單範圍外」，避免互推。
- 四、五兩章為固定模板，內容不要改寫，只調整勾選項。
- 以上為**求才**（企業端，recruit.1111.com.tw）的任務卡與工單格式。
  其他系統（求職端等）若格式不同，等使用者提供範例後另行記錄，不要沿用本模板。

## 求才工單撰寫檢核（依實際開單經驗累積）

寫完工單後逐項確認：

1. **適用範圍**先跟需求端確認哪些分頁真的要做，不要預設「全部分頁」；範圍改動時，
   驗收條件裡的頁數描述也要一起改。
2. **前後端分開開單**。前端單只寫畫面與互動；資料判定、值的保存回填、匯出套用篩選、
   即時查詢機制等列在「本工單範圍外（後端另開工單）」，避免互推。
3. **互動要寫到可驗證**：誰觸發、觸發時帶哪些條件、哪些欄位行為不變、更新後狀態是否保留。
4. **觸發行為以站上既有規則為準**（例：tooltip 桌機 hover／手機點擊、查詢是按鈕觸發還是即時），
   先向需求端確認現況再寫，不要自己假設。
5. **命名交給工程師**：`value`、`id`、class 命名與樣式歸屬寫明「可依前端既有規範調整」。
6. **不得更動**要逐項列出既有元件（按鈕位置、查詢列欄位、容器樣式），避免工程師自行判斷。
7. **定義類需求**（例：「有數據」「有主投」的判定）要向需求端確認後才寫，這是後端寫 SQL 的依據。
8. **驗收條件逐條可勾**，且每條都對得到前面的需求內容。
9. 版面異動附截圖紅框；另附一張「樣式沿用來源」圈出現版對應元件，用以說明無 Figma 檔的理由。
